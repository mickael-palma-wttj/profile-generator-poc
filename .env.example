# =============================================================================
# Profile Generator - Environment Variables
# =============================================================================
# Copy this file to .env and fill in your actual values
# See ENV_VARIABLES.md for complete documentation
# =============================================================================

# -----------------------------------------------------------------------------
# Required: Anthropic API Configuration
# -----------------------------------------------------------------------------
# Get your API key from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=your_api_key_here

# -----------------------------------------------------------------------------
# Optional: Anthropic API Settings
# -----------------------------------------------------------------------------
# Claude model to use (default: claude-sonnet-4-20250514)
ANTHROPIC_MODEL=claude-sonnet-4-20250514

# Maximum tokens per API request (default: 4096)
ANTHROPIC_MAX_TOKENS=4096

# Temperature for response creativity 0.0-1.0 (default: 0.7)
ANTHROPIC_TEMPERATURE=0.7

# API request timeout in seconds (default: 60)
#ANTHROPIC_TIMEOUT=60

# -----------------------------------------------------------------------------
# Prompt Source Configuration
# -----------------------------------------------------------------------------
# Where to load prompts from: 'langfuse' or 'file' (default: langfuse)
# - langfuse: Cloud-based with versioning (recommended)
# - file: Local files from prompts/ directory (offline)
PROMPT_SOURCE=langfuse

# -----------------------------------------------------------------------------
# Langfuse Configuration (Required if PROMPT_SOURCE=langfuse)
# -----------------------------------------------------------------------------
# Get your API keys from: https://cloud.langfuse.com/settings
# Public key (starts with pk-lf-)
LANGFUSE_PUBLIC_KEY=your_langfuse_public_key

# Secret key (starts with sk-lf-)
LANGFUSE_SECRET_KEY=your_langfuse_secret_key

# Langfuse API base URL (default: https://cloud.langfuse.com)
# Only change if using self-hosted Langfuse
#LANGFUSE_BASE_URL=https://cloud.langfuse.com

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
# Server port (default: 4567)
PORT=4567

# Rack environment: development, production, test (default: development)
RACK_ENV=development

# -----------------------------------------------------------------------------
# Logging Configuration
# -----------------------------------------------------------------------------
# Log level: DEBUG, INFO, WARN, ERROR, FATAL (default: INFO)
#LOG_LEVEL=INFO

# Log output: stdout, stderr, or file path (default: stdout)
#LOG_OUTPUT=stdout

# -----------------------------------------------------------------------------
# Profile Generation Settings
# -----------------------------------------------------------------------------
# Maximum parallel threads for generation (default: 5)
#MAX_THREADS=5

# Maximum retries for failed API calls (default: 3)
#MAX_RETRIES=3

# -----------------------------------------------------------------------------
# Development/Testing (Optional)
# -----------------------------------------------------------------------------
# Enable debug mode (shows detailed error traces)
#DEBUG=true

# Enable API response caching for testing
#CACHE_ENABLED=false

# Debug directory for saving API responses
#DEBUG_DIR=./debug

# =============================================================================
# Quick Start Examples
# =============================================================================

# Development with Langfuse (recommended):
# ANTHROPIC_API_KEY=sk-ant-...
# PROMPT_SOURCE=langfuse
# LANGFUSE_PUBLIC_KEY=pk-lf-...
# LANGFUSE_SECRET_KEY=sk-lf-...

# Development offline (local files):
# ANTHROPIC_API_KEY=sk-ant-...
# PROMPT_SOURCE=file

# Production:
# ANTHROPIC_API_KEY=sk-ant-...
# PROMPT_SOURCE=langfuse
# LANGFUSE_PUBLIC_KEY=pk-lf-...
# LANGFUSE_SECRET_KEY=sk-lf-...
# RACK_ENV=production
# LOG_LEVEL=INFO
# MAX_THREADS=10

# =============================================================================
# Documentation
# =============================================================================
# For detailed information, see:
# - ENV_VARIABLES.md - Complete environment variable reference
# - SETUP.md - Quick setup guide
# - README.md - Main documentation
# =============================================================================
