{
    "name": "profile-generator-poc-e2e",
    "version": "1.0.0",
    "description": "End-to-end tests for Profile Generator POC",
    "scripts": {
        "test:e2e": "playwright test",
        "test:e2e:ui": "playwright test --ui",
        "test:e2e:debug": "playwright test --debug"
    },
    "devDependencies": {
        "@playwright/test": "^1.40.0",
        "@types/node": "^20.10.0"
    }
}
